name: Request

on:
    workflow_dispatch: null

jobs:
    warp:
        runs-on: ubuntu-latest
        steps:
        - uses: boywithkeyboard/warp@v1
        - run: |
            curl -vv http://mail.szepe.net:8080/reqy
